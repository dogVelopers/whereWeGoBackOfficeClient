import { DefaultTheme } from 'styled-components';

// css 전역 변수 or 전체 화면에 적용되는 color 혹은 design 지정.
const theme: DefaultTheme = {
  colors: {
    black: '#111111',
  },

  fontStyles: {
    style: 'Noto Sans KR, sans-serif',
  },
};

export default theme;
